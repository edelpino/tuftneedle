<%- @orders.each do |order|%>
	<tr>
		<td><%=link_to order.id, order_path(order)%></td>
		<td><%=order.customer_name%></td>
		<td><%=order.sales_channel.try(:name)%></td>
		<td><%= order.created_at.strftime("%m/%d/%Y")%></td>
	</tr>
<%end%>